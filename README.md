# travel-diary
Personal travel diary.

Open the site through a local web server so the page can fetch the markdown diary files in `data/travel-diary/`.

## Structure

- `data/travel_data.json`: travel entry metadata used by the diary UI.
- `data/travel-diary/`: markdown diary content for each entry.
- `data/photos/`: image folders referenced by the data file.
